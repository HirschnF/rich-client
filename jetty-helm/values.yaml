# # Kubernetes Namespace für die App
# namespace: consult-summit-2025
# helm upgrade --install jetty-usifhirsch ./jetty-helm --namespace consult-summit-2025 --set user="" --set pw=""
namespace: consult-summit-2025

# Benutzer und PW werden zusammengefasst und in secret codiert
user: ""
pw: ""
# #Username of Consultant
# # user: usifhirsch
# # userpw: changeIt
# guacamole:
#   username: usifhirsch
#   password: geheim123

# rdp:
#   #host: 
#   #rc-usifhirsch-svc.consult-summit-2025.k8s-dev004.aspera.usu.grp
#   #rc-usifhirsch-svc.consult-summit-2025.svc.cluster.local
#   #port: 3389
#   username: usifhirsch
#   password: rdppass

enablePersistence: false

image:
  repository: frank1977/jetty-rich-client
  tag: usm5.5.01.53
# funktionierte gut, ohne upload server    tag: usm5.5.01.08
#.05 hat im Web funktioniert

  pullPolicy: Always


imagePullSecrets:
  - name: usu-regcred

ingress:
  enabled: true
  className: nginx
  certManager: true
  annotations:
    cert-manager.io/cluster-issuer: usu-group-issuer
  hostname: rc.consult-summit-2025.k8s-dev004.aspera.usu.grp
  tls: true

storage:
  size: 1Gi
  storageClassName: vsphere-csi-sc

resources:
  limits:
    memory: 4Gi
  requests:
    cpu: 1000m
    memory: 2Gi

# depends on the USC Package secrets.yaml
db:
  host: usm-mariadb.consult-summit-2025.svc.cluster.local
  port: "3306"
  dbase: usm
  qualifier: ""
  dbuser: root
  dbpassword: changeMerCKUYdiu2EIk
  
  dbaseexport: usm_export
  dbasectm: usmctm
  dbasehistory: usm_history
  dbasetest: usm_archive
