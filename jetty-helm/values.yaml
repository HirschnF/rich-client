# # Kubernetes Namespace für die App
# namespace: consult-usimheinz-55
# helm upgrade --install jetty-usimheinz ./myapp-helm --namespace consult-usimheinz-55
namespace: consult-usimheinz-55

#Username of Consultant
user: usimheinz
userpw: mv

enablePersistence: false

image:
  repository: frank1977/jetty-rich-client
  tag: usm5.5.01.15
# funktionierte gut, ohne upload server    tag: usm5.5.01.08
#.05 hat im Web funktioniert

  pullPolicy: Always


imagePullSecrets:
  - name: usu-regcred

ingress:
  enabled: true
  className: nginx
  certManager: true
  annotations:
    cert-manager.io/cluster-issuer: usu-group-issuer
  hostname: rc.consult-usimheinz-55.k8s-dev001.aspera.usu.grp
  tls: true

storage:
  size: 1Gi
  storageClassName: vsphere-csi-sc

resources:
  limits:
    memory: 4Gi
  requests:
    cpu: 1000m
    memory: 2Gi

# depends on the USC Package secrets.yaml
db:
  host: usm-mariadb.consult-usimheinz-55.svc.cluster.local
  port: "3306"
  dbase: usm
  qualifier: ""
  dbuser: usm
  dbpassword: changeMeP1TSgsF0CQeB
  
  dbaseexport: usm_export
  dbasectm: usmctm
  dbasehistory: usm_history
  dbasetest: usm_archive